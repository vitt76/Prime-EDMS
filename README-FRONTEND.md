# 🚀 DAM Frontend - Инструкция по запуску

## ⚠️ ВАЖНО: Запуск команд

**Все команды npm должны запускаться из директории `frontend/`!**

### ✅ Способ 1: Использовать скрипт (РЕКОМЕНДУЕТСЯ)

Из корневой директории `C:\DAM\Prime-EDMS`:

```powershell
# Используйте скрипт run-frontend.ps1
.\run-frontend.ps1 test         # Запуск тестов
.\run-frontend.ps1 storybook     # Запуск Storybook
.\run-frontend.ps1 dev           # Запуск dev сервера
.\run-frontend.ps1 install       # Установка зависимостей
```

### ✅ Способ 2: Перейти в директорию frontend

```powershell
# 1. Перейдите в директорию frontend
cd frontend

# 2. Установите зависимости (если еще не установлены)
npm install

# 3. Запустите нужную команду
npm run dev          # Запуск dev сервера
npm run storybook    # Запуск Storybook
npm run test         # Запуск тестов
```

### ❌ Неправильный способ (из корневой директории):

```powershell
# ❌ Это НЕ сработает:
npm run storybook    # Ошибка: package.json не найден
```

## 📋 Быстрый старт

1. **Перейдите в директорию frontend:**
   ```powershell
   cd frontend
   ```

2. **Установите зависимости:**
   ```powershell
   npm install
   ```
   
   Это установит все зависимости, включая:
   - Vue 3, TypeScript, Vite
   - Tailwind CSS
   - Pinia, Vue Router
   - Storybook
   - Vitest, Playwright
   - И другие...

3. **Запустите dev сервер:**
   ```powershell
   npm run dev
   ```
   
   Откройте браузер: http://localhost:5173

4. **Запустите Storybook:**
   ```powershell
   npm run storybook
   ```
   
   Откройте браузер: http://localhost:6006

5. **Запустите тесты:**
   ```powershell
   npm run test         # Unit тесты
   npm run test:e2e     # E2E тесты (требует запущенный dev сервер)
   ```

## 🔧 Доступные команды

Все команды запускаются из директории `frontend/`:

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск dev сервера (http://localhost:5173) |
| `npm run build` | Сборка для production |
| `npm run preview` | Просмотр production сборки |
| `npm run lint` | Проверка кода ESLint |
| `npm run format` | Форматирование кода Prettier |
| `npm run type-check` | Проверка типов TypeScript |
| `npm run test` | Запуск unit тестов |
| `npm run test:ui` | Запуск тестов с UI |
| `npm run test:coverage` | Запуск тестов с coverage |
| `npm run test:e2e` | Запуск E2E тестов (Playwright) |
| `npm run storybook` | Запуск Storybook (http://localhost:6006) |
| `npm run build-storybook` | Сборка Storybook для деплоя |

## 📁 Структура проекта

```
Prime-EDMS/
├── frontend/              ← ВСЕ команды запускаются отсюда!
│   ├── package.json       ← npm команды работают здесь
│   ├── src/
│   ├── tests/
│   └── ...
└── mayan/                 ← Django backend (не трогаем)
```

## ❌ Частые ошибки

### Ошибка: "Could not read package.json"
**Причина:** Вы находитесь не в директории `frontend/`  
**Решение:** 
```powershell
cd frontend
npm run [команда]
```

### Ошибка: "Module not found"
**Причина:** Зависимости не установлены  
**Решение:**
```powershell
cd frontend
npm install
```

### Ошибка: "Port 5173 already in use"
**Причина:** Порт занят другим процессом  
**Решение:** Измените порт в `vite.config.ts` или остановите другой процесс

## 💡 Совет

Создайте алиас в PowerShell для быстрого перехода:

```powershell
# Добавьте в профиль PowerShell ($PROFILE)
function cdf { cd C:\DAM\Prime-EDMS\frontend }

# Использование:
cdf
npm run dev
```

## 📚 Дополнительная документация

- `frontend/README.md` - Полная документация проекта
- `frontend/QUICKSTART.md` - Быстрый старт
- `frontend/CHECK.md` - Проверка установки

