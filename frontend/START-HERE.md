# 🎯 НАЧНИТЕ ОТСЮДА

## ⚠️ КРИТИЧЕСКИ ВАЖНО

**ВСЕ команды npm должны запускаться из директории `frontend/`!**

## ✅ Правильная последовательность действий:

### 1. Откройте PowerShell и перейдите в директорию frontend:

```powershell
cd C:\DAM\Prime-EDMS\frontend
```

### 2. Убедитесь, что зависимости установлены:

```powershell
# Если node_modules не существует, выполните:
npm install
```

### 3. Запустите нужную команду:

```powershell
# Разработка
npm run dev              # → http://localhost:5173

# Storybook (компоненты)
npm run storybook        # → http://localhost:6006

# Тесты
npm run test             # Unit тесты
npm run test:e2e         # E2E тесты

# Проверка кода
npm run lint             # ESLint
npm run type-check       # TypeScript
```

## ❌ ЧТО НЕ ДЕЛАТЬ:

```powershell
# ❌ НЕ запускайте из корневой директории:
cd C:\DAM\Prime-EDMS
npm run storybook        # ❌ ОШИБКА: package.json не найден

# ✅ ВСЕГДА переходите в frontend:
cd C:\DAM\Prime-EDMS\frontend
npm run storybook        # ✅ РАБОТАЕТ!
```

## 🚀 Быстрая проверка:

```powershell
# 1. Перейдите в frontend
cd C:\DAM\Prime-EDMS\frontend

# 2. Проверьте, что package.json существует
Test-Path package.json    # Должно вернуть: True

# 3. Запустите Storybook
npm run storybook

# 4. Откройте браузер: http://localhost:6006
```

## 📝 Если что-то не работает:

1. **Проверьте текущую директорию:**
   ```powershell
   Get-Location
   # Должно быть: C:\DAM\Prime-EDMS\frontend
   ```

2. **Проверьте наличие package.json:**
   ```powershell
   Test-Path package.json
   # Должно быть: True
   ```

3. **Переустановите зависимости:**
   ```powershell
   cd C:\DAM\Prime-EDMS\frontend
   Remove-Item -Recurse -Force node_modules
   npm install
   ```

## 📚 Дополнительная информация:

- Полная документация: `README.md`
- Быстрый старт: `QUICKSTART.md`
- Проверка установки: `CHECK.md`

