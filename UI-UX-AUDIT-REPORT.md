# UI/UX Audit Report

**Дата:** 26 ноября 2025  
**Автор:** ведущий дизайнер по продукту  
**База:** `frontend/` + эталонные сценарии Bynder / Canto / Cloudinary  

## 1. Scorecard

| Область анализа | Оценка (/10) | Ключевой комментарий |
| :--- | :---: | :--- |
| Навигация и архитектура | 8 | `App.vue` + `router/index.ts` дают понятный skeleton, but sidebar actions hidden on mobile. |
| Поиск и фильтрация | 9 | `SearchBar.vue` + `FiltersPanel.vue` обеспечивают instant results и фасетный набор, нужен highlight результатов. |
| Галерея и массовые действия | 8 | `GalleryView.vue` + `AssetCard.vue` реагируют на выбор, предусмотрена virtual scroll, но TODO-события ещё не реализованы. |
| Коллекции | 7 | `CollectionsPage.vue` предлагает дерево + модалки, но UX drag-and-drop и feedback требуют доработки для 1000+ элементов. |
| Страница ассета | 7 | `AssetDetailPage.vue` хорошо организована, но версии/комментарии и связанные ассеты всё ещё заглушки. |
| Модальные окна | 8 | `UploadModal.vue` и `ShareModal.vue` детализированы, но логика вызова/доступности нуждается в унифицированной структуре. |
| Визуальный дизайн | 8 | Tailwind + custom CSS дают плоский, современный стиль, но единые tokens ещё не документированы. |
| Адаптивность (Mobile UX) | 7 | Основные компоненты адаптируются, но sidebar/filters не оптимизированы под телефоны. |
| **ИТОГО** | **7.8/10** | Современный стек, нужно уточнить детали пользовательских потоков. |

## 2. Краткое резюме

- **Сильные стороны:**  
  1. Модульная архитектура через `App.vue` + `Layout`/`router/index.ts` обеспечивает единый шаблон и guard’ы с авторизацией.  
  2. Поиск создан как instant experience с debounce, keyboard навигацией, recent-сессиями и результатами из `SearchBar.vue`.  
  3. Галерея уже умеет масштабировать работу (virtual scrolling, bulk actions, accessible checkboxes в `GalleryView.vue`).

- **Критические недостатки:**  
  1. `GalleryView.vue` содержит TODO в `handleAssetPreview`, `handleAssetDownload`, `handleAssetMore`, поэтому быстрые действия не работают.  
  2. `CollectionsPage.vue` не показывает визуальную обратную связь drag-and-drop и не отменяет действие при ошибке, что критично для enterprise-сценариев.  
  3. `AssetDetailPage.vue` пока не отображает связанную информацию (versions, related assets) и не интегрируется с метаданными (event handling в `MetadataPanel.vue` закрыт TODO).

## 3. Детальный разбор по областям

### 3.1 Навигация и архитектура
- ✅ Что реализовано хорошо:
  - `App.vue` делит интерфейс на layout/пустой канвас (`showLayout`) — минимальный путь для auth-страниц и стабильный `Header`+`Sidebar`.  
  - `router/index.ts` содержит охрану (`requiresAuth`, `requiresPermission`), auto-redirect и генерацию breadcrumb’ов (главный янтарь для enterprise).  
  - `Sidebar.vue` скрывает/раскрывает меню, поддерживает `collections` + `special` ссылки и quick-cta «Новая папка».
- ⚠️ Зоны для улучшения:
  - Sidebar не адаптивен: на мобильных устройствах он просто сглаживается, но упрощённая структура (пока нет swipe) и иконки без текстов снижают discoverability.  
  - Логика `handleNavigate` в `App.vue` пустая, навигация полностью зависит от `router-link`, нет централизованного telemetry.
- 🔴 Критические UX-проблемы:
  - В админ-модуле нет подсветки активного таба вне `router-link`, навигация по `/admin` может быть непонятной для контекстных задач (нужны breadcrumbs и sticky tabs).  
  - На мобильном фильтры/коллекции не вынесены — пользователю необходимо открывать desktop layout для доступа к most-used разделам.
- 💡 Рекомендации:
  1. Добавить sticky breadcrumbs + contextual tabs в `AdminPage.vue`, чтобы из любой ветки был понятен scope.  
  2. Реализовать responsive “quick switch” (кнопка + dropdown) в `Header.vue` для быстрого перехода между Dashboard, DAM, Collections и Reports.  
  3. Вынести логику навигации/telemetry в `uiStore`, чтобы можно было логировать переходы (см. `useUIStore` вызовы).

### 3.2 Поиск и фильтрация
- ✅ Что реализовано хорошо:
  - `SearchBar.vue` поддерживает debounce 300ms, keyboard navigation, recent searches, loading/error states и instant dropdown (вкладка Results).  
  - `FiltersPanel.vue` покрывает фасетный поиск (тип, размер, теги, кастомные метаданные) с возможностью preview активных фильтров и quick reset.
- ⚠️ Зоны для улучшения:
  - Результаты не подсвечиваются по совпадению (нет `highlight`), а также отсутствует inline-предпросмотр (tooltip + color-coded badges).  
  - Компонент `FiltersPanel.vue` не запоминает открытые/закрытые секции и требует прокрутки при большом количестве кастомных фильтров.
- 🔴 Критические UX-проблемы:
  - Нет команды «Save filter view» → пользователи не могут сохранять сложные наборы из 10+ фильтров, что критично для многокомандных workflow.  
  - Отсутствует визуальная история поиска/фильтров внутри `GalleryView.vue`, то есть при переходе назад пользователь теряет предыдущий контекст.
- 💡 Рекомендации:
  1. Добавить `highlight` строк в `SearchResults.vue`, сопоставляя с `debouncedQuery`.  
  2. Сохранить состояние фильтров в URL (query params) и предложить «Saved view» (кнопка в Toolbar).  
  3. Внедрить quick search chips над галереей, повторно используя `activeFiltersSummary`.

### 3.3 Галерея и массовые действия
- ✅ Что реализовано хорошо:
  - `GalleryView.vue` переключается на virtual scroll при >100 ассетов, показывает skeleton-скаффолды, управляет bulk toolbar.  
  - `AssetCard.vue` имеет lazy loading, intersection observer, WebP, hover-actions, checkbox и accessible focus.
- ⚠️ Зоны для улучшения:
  - Hover-actions содержат заглушки (`handleAssetPreview`, `handleAssetDownload`, `handleAssetMore` — TODO).  
  - Bulk actions (share/move/download/delete/tag) открывают модалки, но сами действия не прерываются по ESC и не показывают прогресс выполнения.
- 🔴 Критические UX-проблемы:
  - Перемещение/добавление ассетов (bulk move) не показывает целевую коллекцию до подтверждения, пользователь может ошибочно переместить десятки медиа.  
  - При выборе ассетов не сообщается об ограничениях (quota, locked assets) {если backend вернет ошибку, нет inline error}.
- 💡 Рекомендации:
  1. Реализовать настоящий preview/download/more, используя `shareService`/`assetService`.  
  2. Добавить toast с підтвердженням выбора и informer об ограничениях (quota) после bulk action.  
  3. Обновить BulkActions toolbar, чтобы он фиксировался (sticky) и показывал прогресс операции.

### 3.4 Коллекции
- ✅ Что реализовано хорошо:
  - `CollectionsPage.vue` имеет skip-links, быстрое переключение между special collections и tree (`CollectionTree.vue`), CRUD-модалки.  
  - Доступ есть к модалкам create/rename/delete с `Button`, `CreateCollectionModal`, `DeleteConfirmModal`.
- ⚠️ Зоны для улучшения:
  - Не хватает live feedback по drag-and-drop (в `CollectionTree.vue` используются события, но нет placeholder’ов).  
  - При открытии collection tree отсутствует фильтрация/поиск по названию (когда >1000 nodes, user теряет контекст).
- 🔴 Критические UX-проблемы:
  - Нет undo/redo для операций с коллекциями; ошибка при удалении с потомками критична и должна быть легко откатима.  
  - Нет индикатора spend (“Выделено 3/5 коллекций”), чтобы пользователь понимал, куда попадёт ассет.
- 💡 Рекомендации:
  1. Добавить `highlighted drop target` и контур при drag внутри `CollectionTree.vue`, с подсказкой «Drop to move asset».  
  2. Внедрить search-input над деревом (typeahead) и breadcrumbs.  
  3. Сохранить состояние открытых веток (localStorage) и добавить undo для delete/move.

### 3.5 Страница ассета
- ✅ Что реализовано хорошо:
  - `AssetDetailPage.vue` разделена на preview pan (image/video fallback), zoom controls, metadata panel (`MetadataPanel.vue`), комментарии и версии.  
  - UI соответствует best practice: sticky metadata, быстрые actions download/share, кнопки `zoom` и `next/prev`.
- ⚠️ Зоны для улучшения:
  - `relatedAssets`, `handleAssetPreview`, `handleShare`, `handleVersionSelect` пока логируют TODO, что рушит опыт просмотра.  
  - Версии/комментарии не грузятся автоматически; `MetadataPanel` весь резервер (см. emit-ы `@version-select`, `@comment-added`), но требуется визуализация.
- 🔴 Критические UX-проблемы:
  - Отсутствие timeline версий и с версионным diff-ом может ввести в ступор при enterprise approval workflows.  
  - Нет inline-валидации лицензионных прав (rights metadata) — пользователь получает только `metadata.status`.
- 💡 Рекомендации:
  1. Интегрировать version history + comment thread (используя `VersionHistory.vue`, `CommentsThread.vue`).  
  2. Добавить переключатель «права доступа» / «usage limits» в `MetadataPanel`.  
  3. Разместить related assets slider под preview и загрузить data из store (используя `assetStore.assets`).

### 3.6 Модальные окна
- ✅ Что реализовано хорошо:
  - `UploadModal.vue` предлагает drag-and-drop, validation, retry/cancel, прогрессы per-file + overall, auto notification.  
  - `ShareModal.vue` предоставляет permissions, expiration, password, active link table и revoke/clipboard.  
  - Другие модалки (`CreateCollectionModal`, `DeleteConfirmModal`, `Bulk*Modal`) придерживаются единого дизайна `Modal.vue`.
- ⚠️ Зоны для улучшения:
  - Все модалки вызываются через `uiStore.openModal`, но нет documentated modal stack; могут возникнуть конкурирующие overlays (upload + share).  
  - `UploadModal.vue` не показывает draft/processing state после загрузки (backend может генерировать derivatives).
- 🔴 Критические UX-проблемы:
  - Нет fallback для API failures: если `uploadAsset` возвращает 500, пользователь видит только `uiStore` notification, нельзя повторить без закрытия.  
  - Share Modal не показывает пожизненные ограничения (limit per user), а также token скрыт по умолчанию без объяснения.
- 💡 Рекомендации:
  1. Добавить modal manager (stack) + ensure only one `Modal` фон активен.  
  2. Расширить `UploadModal` на post-upload state (generating thumbnails).  
  3. В `ShareModal` добавить helper текст и confirm before revoke/share.







